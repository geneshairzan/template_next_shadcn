import UI from "@ui";

export default function App(props) {
  return (
    <UI.Col center p={2}>
      <UI.Text variant="h2" color="primary">
        Chat
      </UI.Text>
    </UI.Col>
  );
}
