import UI from "@ui";

export default function Doc(props) {
  return (
    <UI.Col>
      <UI.Text variant="h2" color="primary">
        Users can use:
        <br />✅ variables (let, const)
        <br />✅ loops (for, while)
        <br />✅ conditions (if)
        <br />✅ functions ✅ math
        <br />✅ add adiotnal doc
      </UI.Text>
    </UI.Col>
  );
}
